/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['uploadthing.com', 'utfs.io', 'ui-avatars.com', 'images.unsplash.com', 'picsum.photos'],
  },
}

module.exports = nextConfig
